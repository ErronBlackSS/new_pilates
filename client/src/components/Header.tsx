import { ROUTES } from '../utils/constance';

const Header = () => {
    return (
        <div className='text-3xl font-bold underline'>
            Hello world!
        </div>
    );
};

export default Header;